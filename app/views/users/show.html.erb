<section class="section--padding-bottom-small">
  <div class="container">
    <div class="row">
      <div class="col-sm-4">
        <div class="post">
          <div class="image"><a href="#">
            <% user_photo = @user.facebook_picture_url || (@user.photo? ? cl_image_path( @user.photo.path, :crop => "fill") : "http://via.placeholder.com/350x150") %>
            <%= cl_image_tag user_photo, class: "img-responsive" %>
            <h1><%= @user.first_name%></h1>
          </div>
        </div>
        </div>
        <div class="col-sm-4">
          <div class="post">
            <div class="image">
            </div>
          </div>
        </div>
          <div class="col-sm-4">
            <div class="post">
              <div class="image">
              </div>
                <% if @user.role_cleaner? %>
                <h1><%= @user.hourly_rate %>€ <sup>/h</sup> </h1>
                <h3><i class="pe-7s-piggy"></i>-50% discount for additional hours</h3>
                <p class="post__intro">
                  <input type="date" name="date">
                </p>
                <p> <%= link_to "Book cleaning", new_user_booking_path(@user.id), class: "btn btn-ghost" %></p>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </section>
      <!--   *** SERVICES ***-->
      <section class="background-gray-lightest">
        <div class="container clearfix">
          <div class="row services">
            <div class="col-md-12">
              <h2>Services</h2>
              <div class="row">
                <div class="col-sm-4">
                  <div class="box box-services">
                    <div class="icon"><i class="pe-7s-smile"></i></div>
                    <h4 class="services-heading">General Cleaning</h4>
                  </div>
                </div>
                <div class="col-sm-4">
                  <div class="box box-services">
                    <div class="icon"><i class="pe-7s-smile"></i></div>
                    <h4 class="services-heading">Ironing</h4>
                  </div>
                </div>
                <div class="col-sm-4">
                  <div class="box box-services">
                    <div class="icon"><i class="pe-7s-smile"></i></div>
                    <h4 class="services-heading">Pet Cleaning</h4>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class="background-gray-lightest">
        <div class="container clearfix">
          <div class="row services">
            <div class="col-md-12">
              <h2><%= @user.first_name%>'s working area</h2>
              <div id="map" style="width: 100%; height: 600px;"></div>
              <% content_for(:after_js) do %>
                <%= javascript_tag do %>
                  var handler = Gmaps.build('Google');
                  handler.buildMap({ internal: { id: 'map' } }, function() {
                    markers = handler.addMarkers(<%= raw @hash.to_json %>);
                    handler.bounds.extendWith(markers);
                    handler.fitMapToBounds();
                    if (markers.length == 0) {
                      handler.getMap().setZoom(2);
                    } else if (markers.length == 1) {
                      handler.getMap().setZoom(14);
                    }
                  });
                <% end %>
              <% end %>
            </div>
          </div>
        </div>
      </section>
